server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Handle Angular routing (HTML5 pushState)
    location / {
        try_files $uri $uri/ /index.html;
    }
}